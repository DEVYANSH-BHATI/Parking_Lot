<div class="form container">

      <% if current_user.is_Admin %>
        <h1>New vehicle</h1>
            <%= p current_user.is_Admin.inspect%>
            <%= render "form", vehicle: @vehicle %>
          <br>
        <% else %>
        <%= render "shared/not_admin"%>
      <% end %>
     <div>
       <%= link_to "Back to vehicles", vehicles_path %>
     </div>
</div>
